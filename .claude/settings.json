{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cmd /c \"where pwsh >NUL 2>&1 && pwsh -NoProfile -ExecutionPolicy Bypass -File .claude/hooks/cleanup-session.ps1 || echo [claude-stop] pwsh not found, skipping cleanup-session.ps1\""
          }
        ]
      }
    ]
  }
}
