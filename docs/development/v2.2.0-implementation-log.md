# v2.2.0 Implementation Log

**Branch**: feat/v2.2.0
**Start date**: 2026-03-01
**Spec**: [docs/specs/v2.2.0-requirements.md](../specs/v2.2.0-requirements.md)

## Approach

TDD (tests → implementation → docs) for all 11 features.
Each feature produces 3 commits: `test`, `feat`, `docs`.

---

## F-01 — VHDX Disk Compaction

_Status: **Complete**_

### Key Decisions

- VHDX path resolved from registry `HKCU:\Software\Microsoft\Windows\CurrentVersion\Lxss\{guid}\BasePath`
- `fstrim -av` executed inside instance before compaction to zero deleted blocks; non-fatal if fstrim not present
- Prefer `Optimize-VHD -Mode Full` (Hyper-V module); fall back to `diskpart compact vdisk` when unavailable
- `diskpart` requires administrator elevation — detected via `Test-AdminPrivilege` helper (WindowsPrincipal.IsInRole)
- Auto-stop running instance before compact, auto-restart after if it was running
- `CompactInstanceAsync` in `WslManagerService` delegates to `Compress-DistroNexusInstance` cmdlet with `VeryLongOperationTimeoutSeconds` (300s)
- Helper functions `Get-InstanceVhdxPath` and `Test-AdminPrivilege` live in the same cmdlet file (module-private)

### Files Changed

| File | Change |
|------|--------|
| `tests/PowerShell/Unit/Public/Compress-DistroNexusInstance.Tests.ps1` | New — Pester unit tests |
| `src/Client/DistroNexus.Tests/Services/CompactInstanceTests.cs` | New — C# unit tests |
| `src/PowerShell/Public/Compress-DistroNexusInstance.ps1` | New — cmdlet implementation |
| `src/PowerShell/DistroNexus.psd1` | Modified — added to FunctionsToExport |
| `src/Client/DistroNexus.Core/Interfaces/IWslManagerService.cs` | Modified — added CompactInstanceAsync |
| `src/Client/DistroNexus.Core/Services/WslManagerService.cs` | Modified — implemented CompactInstanceAsync |

---

## F-02 — Docker Desktop Integration

_Status: **Complete**_

### Key Decisions

- Docker settings path: `$env:APPDATA\Docker\settings-store.json` (4.30+) with fallback to `settings.json`
- Docker installation detected via `$env:LOCALAPPDATA\Docker\Docker Desktop.exe`; registry fallback
- `integratedWslDistros` JSON array read/written atomically (full JSON rewrite)
- WSL v1 instances rejected in `Enable-DistroNexusDockerIntegration` with descriptive error
- Reserved distros `docker-desktop` and `docker-desktop-data` always return `Unavailable`
- `DockerIntegrationStatus` enum lives in `Services/` (not `Models/`) alongside the service
- `DockerIntegrationService.IsDockerDesktopInstalledAsync` is `virtual` to support test subclassing

### Files Changed

| File | Change |
|------|--------|
| `tests/PowerShell/Unit/Public/Get-DistroNexusDockerIntegration.Tests.ps1` | New — Pester unit tests |
| `src/Client/DistroNexus.Tests/Services/DockerIntegrationServiceTests.cs` | New — C# unit tests |
| `src/PowerShell/Public/Get-DistroNexusDockerIntegration.ps1` | New — cmdlet |
| `src/PowerShell/Public/Enable-DistroNexusDockerIntegration.ps1` | New — cmdlet |
| `src/PowerShell/Public/Disable-DistroNexusDockerIntegration.ps1` | New — cmdlet |
| `src/PowerShell/DistroNexus.psd1` | Modified — added 3 cmdlets to FunctionsToExport |
| `src/Client/DistroNexus.Core/Interfaces/IDockerIntegrationService.cs` | New — interface |
| `src/Client/DistroNexus.Core/Services/DockerIntegrationStatus.cs` | New — enum |
| `src/Client/DistroNexus.Core/Services/DockerIntegrationService.cs` | New — implementation |
| `src/Client/DistroNexus.Desktop/App.xaml.cs` | Modified — registered IDockerIntegrationService |

---

## E-01 — Instance Export and Import

_Status: **Complete**_

### Key Decisions

- Export guards: check instance exists, error if running (use `-Force` to auto-stop), validate destination
- Import guards: validate source file exists, error if name already registered, create install path if missing
- Default export filename: `<Name>-<yyyyMMdd>.tar` when Destination is a directory
- `ExportInstanceAsync` and `ImportInstanceAsync` added to `IWslManagerService` and implemented in `WslManagerService`
- Both delegate to PS cmdlets via `ExecuteModuleCmdletAsync` with `VeryLongOperationTimeoutSeconds` (300s)
- `GetInstanceConfigAsync` and `SetSparseModeAsync` stubs also added to interface (consumed by E-03)

### Files Changed

| File | Change |
|------|--------|
| `tests/PowerShell/Unit/Public/Export-DistroNexusInstance.Tests.ps1` | New — Pester unit tests |
| `src/Client/DistroNexus.Tests/Services/ExportImportInstanceTests.cs` | New — C# unit tests |
| `src/PowerShell/Public/Export-DistroNexusInstance.ps1` | New — cmdlet |
| `src/PowerShell/Public/Import-DistroNexusInstance.ps1` | New — cmdlet |
| `src/PowerShell/DistroNexus.psd1` | Modified — added 2 cmdlets |
| `src/Client/DistroNexus.Core/Interfaces/IWslManagerService.cs` | Modified — added export/import/config/sparse methods |
| `src/Client/DistroNexus.Core/Services/WslManagerService.cs` | Modified — implemented new methods |

---

## E-02 — .wslconfig Global Configuration Editor

_Status: **Complete**_

### Key Decisions

- INI parsing is line-based; only `[wsl2]` section is read/written; other sections and comments preserved
- `Set-DistroNexusWslConfig`: only supplied parameters are updated; unknown keys in [wsl2] preserved
- Memory > 80% of host RAM triggers `Write-Warning` (uses `[System.Environment]::WorkingSet`)
- `WslConfig` model placed in `Models/` (not `Services/`) for consistency with other models
- `WslConfigService` accepts an optional `userProfileDir` ctor arg for testability
- `GetHostSpecsAsync` uses `GC.GetGCMemoryInfo().TotalAvailableMemoryBytes` for RAM estimation

### Files Changed

| File | Change |
|------|--------|
| `tests/PowerShell/Unit/Public/Get-DistroNexusWslConfig.Tests.ps1` | New — Pester unit tests |
| `src/Client/DistroNexus.Tests/Services/WslConfigServiceTests.cs` | New — C# unit tests |
| `src/PowerShell/Public/Get-DistroNexusWslConfig.ps1` | New — cmdlet |
| `src/PowerShell/Public/Set-DistroNexusWslConfig.ps1` | New — cmdlet |
| `src/PowerShell/DistroNexus.psd1` | Modified — added 2 cmdlets |
| `src/Client/DistroNexus.Core/Models/WslConfig.cs` | New — model |
| `src/Client/DistroNexus.Core/Interfaces/IWslConfigService.cs` | New — interface |
| `src/Client/DistroNexus.Core/Services/WslConfigService.cs` | New — implementation |
| `src/Client/DistroNexus.Desktop/App.xaml.cs` | Modified — registered IWslConfigService |

---

## E-03 — Instance-Level Resource Configuration

_Status: **Complete**_

### Key Decisions

- `Get-DistroNexusInstanceConfig` reads `SparseVhd` registry value from Lxss key; falls back gracefully
- Also reads global `.wslconfig` via `Get-DistroNexusWslConfig` to expose GlobalMemory/GlobalCPUs
- `Set-DistroNexusInstanceSparseMode` calls `wsl --manage $Name --set-sparse $true|false`
- WSL v1 guard: error with `DistroNexus.WslVersionTooLow`
- `GetInstanceConfigAsync` and `SetSparseModeAsync` already added to interface in E-01; implementations delegate to PS cmdlets

### Files Changed

| File | Change |
|------|--------|
| `tests/PowerShell/Unit/Public/Get-DistroNexusInstanceConfig.Tests.ps1` | New — Pester unit tests |
| `src/Client/DistroNexus.Tests/Services/InstanceConfigTests.cs` | New — C# unit tests |
| `src/PowerShell/Public/Get-DistroNexusInstanceConfig.ps1` | New — cmdlet |
| `src/PowerShell/Public/Set-DistroNexusInstanceSparseMode.ps1` | New — cmdlet |
| `src/PowerShell/DistroNexus.psd1` | Modified — added 2 cmdlets |

---

## E-04 — Instance Backup Scheduling

_Status: Pending_

---

## E-05 — Port Forwarding Visualization

_Status: Pending_

---

## E-06 — Instance Tagging and Grouping

_Status: Pending_

---

## E-07 — Proactive Cache Invalidation

_Status: Pending_

---

## E-08 — Decouple PowerShell Service Layer

_Status: Pending_

---

## E-09 — Unified Error Code System

_Status: Pending_
