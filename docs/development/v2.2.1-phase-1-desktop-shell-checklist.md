# v2.2.1 Phase 1 — Desktop Shell & InstanceDetailDialog

**Phase**: 1 of 7  
**Status**: Not Started  
**Dependencies**: Phase 0  
**Blocks**: Phase 2, 4, 5  
**Requirements**: A-01 through A-07  
**Design References**: [Design §2.1, §2.2, §2.3.2, §3.1, §3.2, §3.3.1](../specs/v2.2.1-design.md)

---

## Goal

Build the structural UI foundation that all feature groups depend on: the `InstanceDetailDialog` skeleton, multi-select infrastructure, and updated context menu and toolbar.

---

## Tasks

### P1-1: Create `InstanceDetailDialog` window

**Req**: A-01, A-02, A-04

- [ ] Create `src/Client/DistroNexus.Desktop/Views/InstanceDetailDialog.xaml`
  - `ui:FluentWindow`, initial size 780 × 540, min 640 × 480
  - `WindowBackdropType="Mica"`, `ExtendsContentIntoTitleBar="True"`
  - Header strip: distro icon, instance name, state badge, tag chips placeholder, Edit Tags button placeholder
  - `TabControl` with 5 tabs: Disk, Resources, Integrations, Network, Backup
  - Footer: progress area (hidden by default) + Close button
- [ ] Create `src/Client/DistroNexus.Desktop/Views/InstanceDetailDialog.xaml.cs` (code-behind, minimal)
- [ ] Dialog receives `WslInstanceViewModel` on construction and sets it as DataContext
- [ ] Verify Mica backdrop renders on Windows 11
- [ ] Verify `{DynamicResource}` used for all color/brush references (A-07)
- [ ] Verify dialog is resizable and respects min size

**Acceptance**:
- [ ] Dialog opens, shows 5 tab headers, displays instance name in header
- [ ] Each tab shows placeholder content ("Coming soon" or empty)
- [ ] Close button dismisses the dialog

---

### P1-2: Create `InstanceDetailViewModel`

**Req**: A-03, A-04

- [ ] Create `src/Client/DistroNexus.Desktop/ViewModels/InstanceDetailViewModel.cs`
  - Constructor receives: `WslInstanceViewModel`, `IWslManagerService`, `IDockerIntegrationService`, `INetworkService`, `IBackupService`, `IWslConfigService`, `ITagService`, `IDialogService`
  - Exposes `Instance` property (the `WslInstanceViewModel`)
  - Creates and exposes tab ViewModels as readonly properties:
    - `DiskTab: DiskTabViewModel`
    - `ResourcesTab: ResourcesTabViewModel`
    - `IntegrationsTab: IntegrationsTabViewModel`
    - `NetworkTab: NetworkTabViewModel`
    - `BackupTab: BackupTabViewModel`
  - `CloseCommand` (closes the dialog)
  - `SelectedTabIndex` property with `OnTabChanged` handler for lazy tab initialization

**Acceptance**:
- [ ] All 5 tab VMs are created
- [ ] Switching tabs triggers lazy `InitializeAsync` on first visit

---

### P1-3: Create stub tab ViewModels

**Req**: A-03

Create 5 stub ViewModels (will be filled in Phases 2, 4, 5):

- [ ] `src/Client/DistroNexus.Desktop/ViewModels/Tabs/DiskTabViewModel.cs`
  - Inherits `ObservableObject`
  - Receives `WslInstanceViewModel`, `IWslManagerService`, `IDialogService`
  - Stub `InitializeAsync()` method
- [ ] `src/Client/DistroNexus.Desktop/ViewModels/Tabs/ResourcesTabViewModel.cs`
  - Receives `WslInstanceViewModel`, `IWslManagerService`, `IWslConfigService`, `IDialogService`
  - Stub `InitializeAsync()` method
- [ ] `src/Client/DistroNexus.Desktop/ViewModels/Tabs/IntegrationsTabViewModel.cs`
  - Receives `WslInstanceViewModel`, `IDockerIntegrationService`, `IDialogService`
  - Stub `InitializeAsync()` method
- [ ] `src/Client/DistroNexus.Desktop/ViewModels/Tabs/NetworkTabViewModel.cs`
  - Receives `WslInstanceViewModel`, `INetworkService`, `IDialogService`
  - Stub `InitializeAsync()` method
- [ ] `src/Client/DistroNexus.Desktop/ViewModels/Tabs/BackupTabViewModel.cs`
  - Receives `WslInstanceViewModel`, `IBackupService`, `IDialogService`
  - Stub `InitializeAsync()` method

**Acceptance**:
- [ ] All 5 VMs compile and can be instantiated
- [ ] Each has an `InitializeAsync` placeholder

---

### P1-4: Create stub tab UserControls

**Req**: A-03, A-07

- [ ] `src/Client/DistroNexus.Desktop/Controls/Tabs/DiskTabView.xaml` + `.xaml.cs`
- [ ] `src/Client/DistroNexus.Desktop/Controls/Tabs/ResourcesTabView.xaml` + `.xaml.cs`
- [ ] `src/Client/DistroNexus.Desktop/Controls/Tabs/IntegrationsTabView.xaml` + `.xaml.cs`
- [ ] `src/Client/DistroNexus.Desktop/Controls/Tabs/NetworkTabView.xaml` + `.xaml.cs`
- [ ] `src/Client/DistroNexus.Desktop/Controls/Tabs/BackupTabView.xaml` + `.xaml.cs`
- [ ] Each uses `{DynamicResource}` for all brushes (A-07)
- [ ] Each binds to its corresponding tab ViewModel

**Acceptance**:
- [ ] Tabs render placeholder content inside `InstanceDetailDialog`

---

### P1-5: Add "Details" menu item to MoreButton context menu

**Req**: A-01

- [ ] Add `Details…` as the first item in the existing `MoreButton` `ContextMenu` on each instance card in `MainWindow.xaml`
- [ ] Add separator after `Details…`
- [ ] Add `OpenDetailsCommand` to `WslInstanceViewModel`:
  - Creates `InstanceDetailViewModel` with required services (resolved from DI)
  - Creates `InstanceDetailDialog` with the ViewModel
  - Calls `dialog.ShowDialog()`
- [ ] Bind `Details…` menu item to `OpenDetailsCommand`

**Acceptance**:
- [ ] Clicking "Details…" opens the `InstanceDetailDialog` for that instance
- [ ] Dialog shows correct instance name and state

---

### P1-6: Add "Compact Disk" and "Export Instance" menu items (stubs)

**Req**: B-01-1, B-01-6, B-02-1

- [ ] Add `Compact Disk` menu item to `MoreButton` after the Details separator
- [ ] Add `Export Instance…` menu item after Compact Disk
- [ ] Add separator after Export Instance
- [ ] `Compact Disk` visibility bound to `IsWslV2` (hidden for v1 instances per UX-06)
- [ ] Both items bound to stub commands (will be implemented in Phase 2)
- [ ] Verify existing menu items (Move, Rename, etc.) remain in correct order per design:
  ```
  Details…
  ────────
  Compact Disk         (WSL v2 only)
  Export Instance…
  ────────
  Move
  Rename
  Set Credentials
  ────────
  Force Refresh
  ────────
  Remove…
  ```

**Acceptance**:
- [ ] Menu shows all items in correct order
- [ ] Compact Disk hidden for WSL v1 instances
- [ ] Stub commands don't crash when clicked

---

### P1-7: Add "Import Instance" toolbar button

**Req**: B-02-4

- [ ] Add `Import Instance` button (`ui:Button`, Icon: `ArrowImport20`) to dashboard toolbar in `MainWindow.xaml`
- [ ] Position: left group, after existing buttons (Refresh, Install New, Package Manager, Templates, Settings)
- [ ] Bind to stub `ImportInstanceCommand` on `MainViewModel`
- [ ] Stub command does nothing for now (implemented in Phase 2)

**Acceptance**:
- [ ] Import button visible in toolbar
- [ ] Button does not crash when clicked

---

### P1-8: Implement multi-select mode infrastructure

**Req**: A-05, A-06

- [ ] Add to `MainViewModel`:
  - `IsMultiSelectMode` observable property (bool, default false)
  - `ToggleMultiSelectCommand` relay command
  - `CompactSelectedCommand` relay command (stub — implemented in Phase 2)
  - When `IsMultiSelectMode` toggles to false, clear all `IsSelected` flags
- [ ] Add to `WslInstanceViewModel`:
  - `IsSelected` observable property (bool, default false)
- [ ] Add **Select** toggle button (`ui:ToggleButton`, Icon: `CheckboxChecked20`) to dashboard toolbar
  - Bound to `IsMultiSelectMode`
  - Position: after Import button
- [ ] In `MainWindow.xaml` instance card template:
  - Add `CheckBox` bound to `WslInstanceViewModel.IsSelected`
  - Visibility bound to `MainViewModel.IsMultiSelectMode`
- [ ] Add **Compact Selected** button below instance list
  - Visibility: `IsMultiSelectMode && SelectedCount >= 1`
  - Badge shows count of selected instances
  - Bound to `CompactSelectedCommand`

**Acceptance**:
- [ ] Toggle button activates/deactivates multi-select mode
- [ ] Checkboxes appear on cards when mode is active
- [ ] Compact Selected button appears when ≥ 1 instance is selected
- [ ] Exiting multi-select mode clears all selections

---

### P1-9: Add auto-refresh indicator placeholder

**Req**: F-01-4

- [ ] Add `IsAutoRefreshing` observable property (bool) to `MainViewModel`
- [ ] Add small `ui:ProgressRing` (16 px, indeterminate) in toolbar right group
  - Visibility bound to `IsAutoRefreshing`
  - Position: before theme/language controls
- [ ] Does not function yet (wired in Phase 7)

**Acceptance**:
- [ ] `IsAutoRefreshing = true` shows spinner in toolbar
- [ ] `IsAutoRefreshing = false` hides spinner

---

### P1-10: Add localization resource strings for Phase 1

**Req**: UX-07

- [ ] Add to `Resources.resx` (en-US):
  - `MoreMenu_Details` = "Details…"
  - `MoreMenu_CompactDisk` = "Compact Disk"
  - `MoreMenu_ExportInstance` = "Export Instance…"
  - `Toolbar_ImportInstance` = "Import Instance"
  - `Toolbar_SelectMode` = "Select"
  - `Toolbar_CompactSelected` = "Compact Selected ({0})"
  - `InstanceDetail_Title` = "Instance Details — {0}"
  - `InstanceDetail_Close` = "Close"
  - `Tab_Disk` = "Disk"
  - `Tab_Resources` = "Resources"
  - `Tab_Integrations` = "Integrations"
  - `Tab_Network` = "Network"
  - `Tab_Backup` = "Backup"
- [ ] Add corresponding entries to `Resources.zh-CN.resx`

**Acceptance**:
- [ ] No hard-coded strings in any new XAML or ViewModel code
- [ ] All strings display correctly in English

---

## Verification

- [ ] Full solution builds without errors
- [ ] App launches; dashboard renders all instances with updated context menu
- [ ] "Details…" opens `InstanceDetailDialog` with 5 tabs (stub content)
- [ ] Multi-select toggle works: checkboxes appear/disappear, Compact Selected button visibility correct
- [ ] Import button visible in toolbar
- [ ] No regressions in existing functionality (Start/Stop/Terminal/Move/Rename/Remove)

---

## Files Created / Modified

| Action | Path |
|--------|------|
| Create | `src/Client/DistroNexus.Desktop/Views/InstanceDetailDialog.xaml` |
| Create | `src/Client/DistroNexus.Desktop/Views/InstanceDetailDialog.xaml.cs` |
| Create | `src/Client/DistroNexus.Desktop/ViewModels/InstanceDetailViewModel.cs` |
| Create | `src/Client/DistroNexus.Desktop/ViewModels/Tabs/DiskTabViewModel.cs` |
| Create | `src/Client/DistroNexus.Desktop/ViewModels/Tabs/ResourcesTabViewModel.cs` |
| Create | `src/Client/DistroNexus.Desktop/ViewModels/Tabs/IntegrationsTabViewModel.cs` |
| Create | `src/Client/DistroNexus.Desktop/ViewModels/Tabs/NetworkTabViewModel.cs` |
| Create | `src/Client/DistroNexus.Desktop/ViewModels/Tabs/BackupTabViewModel.cs` |
| Create | `src/Client/DistroNexus.Desktop/Controls/Tabs/DiskTabView.xaml` (+.cs) |
| Create | `src/Client/DistroNexus.Desktop/Controls/Tabs/ResourcesTabView.xaml` (+.cs) |
| Create | `src/Client/DistroNexus.Desktop/Controls/Tabs/IntegrationsTabView.xaml` (+.cs) |
| Create | `src/Client/DistroNexus.Desktop/Controls/Tabs/NetworkTabView.xaml` (+.cs) |
| Create | `src/Client/DistroNexus.Desktop/Controls/Tabs/BackupTabView.xaml` (+.cs) |
| Modify | `src/Client/DistroNexus.Desktop/MainWindow.xaml` |
| Modify | `src/Client/DistroNexus.Desktop/ViewModels/MainViewModel.cs` |
| Modify | `src/Client/DistroNexus.Desktop/ViewModels/WslInstanceViewModel.cs` |
| Modify | `src/Client/DistroNexus.Desktop/Properties/Resources.resx` |
| Modify | `src/Client/DistroNexus.Desktop/Properties/Resources.zh-CN.resx` |
