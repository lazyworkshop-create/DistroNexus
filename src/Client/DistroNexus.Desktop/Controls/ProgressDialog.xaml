<ui:FluentWindow x:Class="DistroNexus.Desktop.Controls.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:properties="clr-namespace:DistroNexus.Desktop.Properties"
        mc:Ignorable="d"
        Title="{Binding Title, RelativeSource={RelativeSource Self}}"
        Height="200"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   x:Name="MessageTextBlock"
                   Text="{Binding Message, RelativeSource={RelativeSource AncestorType=Window}}"
                   FontSize="14"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1" VerticalAlignment="Center">
            <ui:ProgressRing x:Name="IndeterminateProgress"
                             IsIndeterminate="True" 
                             Width="50" 
                             Height="50"
                             HorizontalAlignment="Center"
                             Visibility="Collapsed"/>
            
            <StackPanel x:Name="DeterminateProgress" Visibility="Visible">
                <ProgressBar x:Name="ProgressBar"
                                Value="{Binding Progress, RelativeSource={RelativeSource AncestorType=Window}}"
                                Maximum="100"
                                Height="8"/>
                <TextBlock x:Name="ProgressTextBlock"
                           Text="{Binding ProgressText, RelativeSource={RelativeSource AncestorType=Window}}"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="2"
                   x:Name="StatusTextBlock"
                   Text="{Binding StatusMessage, RelativeSource={RelativeSource AncestorType=Window}}"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,10,0,0"/>

        <ui:Button Grid.Row="3"
                   x:Name="CancelButton"
                   Content="{x:Static properties:Resources.Cancel}"
                   HorizontalAlignment="Center"
                   Margin="0,15,0,0"
                   Click="CancelButton_Click"
                   Visibility="{Binding IsCancellable, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</ui:FluentWindow>
