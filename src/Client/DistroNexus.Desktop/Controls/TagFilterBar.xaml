<UserControl x:Class="DistroNexus.Desktop.Controls.TagFilterBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             lex:ResxLocalizationProvider.DefaultAssembly="DistroNexus.Desktop"
             lex:ResxLocalizationProvider.DefaultDictionary="Properties.Resources"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:DistroNexus.Desktop.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">

    <!-- Collapses when no available tags -->
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding AvailableTags.Count}" Value="0">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    <Border Padding="0,6,0,10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Title label -->
                <TextBlock Text="{lex:Loc TagFilter_Title}"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="12"
                           Margin="0,0,8,0"/>

                <!-- Tag pills -->
                <ItemsControl ItemsSource="{Binding AvailableTags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:TagFilterViewModel}">
                            <Border CornerRadius="10"
                                    BorderThickness="1"
                                    Padding="8,2"
                                    Margin="0,0,6,4"
                                    Cursor="Hand"
                                    VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                        <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                                                <Setter Property="Background" Value="{DynamicResource SystemAccentColorSecondaryBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsSelected}"
                                              Visibility="Collapsed"/>
                                    <TextBlock FontSize="12"
                                               VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="{Binding Name}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <!-- × deselect (only when selected) -->
                                    <Button Content="×"
                                            FontSize="11"
                                            Margin="4,0,0,0"
                                            Padding="2,0"
                                            BorderThickness="0"
                                            Background="Transparent"
                                            Cursor="Hand"
                                            VerticalAlignment="Center"
                                            Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibilityConverter}}"
                                            Command="{Binding DataContext.ClearSingleTagFilterCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftClick"
                                                  Command="{Binding DataContext.ToggleTagFilterCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                  CommandParameter="{Binding}"/>
                                </Border.InputBindings>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Action buttons -->
                <ui:Button Content="{lex:Loc TagFilter_ClearAll}"
                           Command="{Binding ClearTagFiltersCommand}"
                           Appearance="Secondary"
                           FontSize="12"
                           Padding="8,2"
                           Margin="4,0,6,4"
                           VerticalAlignment="Center"/>

                <ToggleButton Content="{lex:Loc TagFilter_GroupByTag}"
                              IsChecked="{Binding IsGroupByTag}"
                              Command="{Binding ToggleGroupByTagCommand}"
                              FontSize="12"
                              Padding="8,2"
                              Margin="0,0,0,4"
                              VerticalAlignment="Center"/>
            </WrapPanel>
        </Grid>
    </Border>
</UserControl>
