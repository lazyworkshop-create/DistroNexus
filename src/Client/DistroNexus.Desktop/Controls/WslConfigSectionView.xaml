<UserControl x:Class="DistroNexus.Desktop.Controls.WslConfigSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             lex:ResxLocalizationProvider.DefaultAssembly="DistroNexus.Desktop"
             lex:ResxLocalizationProvider.DefaultDictionary="Properties.Resources">

    <ui:CardExpander Header="{lex:Loc WslConfig_SectionTitle}"
                     Icon="{ui:SymbolIcon Symbol=Settings20}"
                     IsExpanded="False"
                     Expanded="OnExpanded">

        <Grid IsEnabled="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}">
            <StackPanel Margin="0,12,0,0">

                <!-- Loading indicator -->
                <ui:ProgressRing IsIndeterminate="True"
                                 Width="24" Height="24"
                                 Margin="0,0,0,12"
                                 Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <!-- Host info -->
                <TextBlock Text="{Binding HostInfo}"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="12"
                           Margin="0,0,0,16"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}"/>

                <TextBlock Text="{Binding PendingRestart}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ItemsControl ItemsSource="{Binding Fields}">
                    <ItemsControl.Resources>
                        <DataTemplate x:Key="TextSetting"><TextBox Text="{Binding Desired, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsSupported}"/></DataTemplate>
                        <DataTemplate x:Key="BooleanSetting"><ComboBox SelectedValue="{Binding Desired, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" IsEnabled="{Binding IsSupported}"><ComboBoxItem Content="true"/><ComboBoxItem Content="false"/></ComboBox></DataTemplate>
                        <DataTemplate x:Key="EnumSetting"><ComboBox ItemsSource="{Binding Definition.AllowedValues}" SelectedItem="{Binding Desired, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsSupported}"/></DataTemplate>
                        <Style x:Key="SettingEditor" TargetType="ContentControl"><Setter Property="ContentTemplate" Value="{StaticResource TextSetting}"/><Style.Triggers>
                            <DataTrigger Binding="{Binding Definition.Type}" Value="Boolean"><Setter Property="ContentTemplate" Value="{StaticResource BooleanSetting}"/></DataTrigger>
                            <DataTrigger Binding="{Binding Definition.Type}" Value="Enum"><Setter Property="ContentTemplate" Value="{StaticResource EnumSetting}"/></DataTrigger>
                        </Style.Triggers></Style>
                    </ItemsControl.Resources>
                    <ItemsControl.ItemTemplate><DataTemplate><StackPanel Margin="0,2"><Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/><ColumnDefinition Width="220"/></Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Label}" VerticalAlignment="Center"/>
                        <ContentControl Grid.Column="1" Content="{Binding}" Style="{StaticResource SettingEditor}"/>
                        <TextBlock Grid.Column="2" Text="{Binding UnsupportedReason}" Foreground="DarkOrange" TextWrapping="Wrap" Margin="8,0,0,0"/>
                    </Grid><TextBlock Text="{Binding ValidationError}" Foreground="OrangeRed"/></StackPanel></DataTemplate></ItemsControl.ItemTemplate>
                </ItemsControl>
                <Expander Header="{lex:Loc Configuration_CurrentRaw}" Margin="0,8,0,0"><TextBox Text="{Binding CurrentRaw}" IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas" MinHeight="100"/></Expander>
                <Expander Header="{lex:Loc Configuration_DesiredRaw}" Margin="0,4,0,12"><TextBox Text="{Binding DesiredRaw}" IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas" MinHeight="100"/></Expander>

                <!-- Action buttons -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:Button Grid.Column="0"
                               Content="{lex:Loc WslConfig_SaveAndMarkPendingRestart}"
                               Icon="{ui:SymbolIcon Symbol=Save20}"
                               Appearance="Primary"
                               Command="{Binding SaveAndMarkPendingRestartCommand}"
                               Margin="0,0,10,0"/>
                </Grid>

            </StackPanel>
        </Grid>

    </ui:CardExpander>

</UserControl>
