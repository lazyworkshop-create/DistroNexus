<UserControl x:Class="DistroNexus.Desktop.Wizard.Steps.InstallPathStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:DistroNexus.Desktop.Wizard.Steps"
             xmlns:properties="clr-namespace:DistroNexus.Desktop.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance Type=local:InstallPathStep}">
    
    <StackPanel>
        <!-- Instance Name -->
        <TextBlock Text="{x:Static properties:Resources.InstanceNameLabel}"
                   FontSize="14"
                   Margin="0,0,0,8"/>
        <ui:TextBox Text="{Binding Context.InstanceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   PlaceholderText="{x:Static properties:Resources.PlaceholderInstanceName}"
                   Margin="0,0,0,20"/>
        
        <!-- Installation Path -->
        <TextBlock Text="{x:Static properties:Resources.LabelInstallPath}"
                   FontSize="14"
                   Margin="0,0,0,8"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <ui:TextBox Grid.Column="0"
                       Text="{Binding Context.InstallPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       PlaceholderText="{x:Static properties:Resources.PlaceholderInstallPath}"
                       Margin="0,0,10,0"/>
            
            <ui:Button Grid.Column="1"
                      Content="{x:Static properties:Resources.ButtonBrowse}"
                      Command="{Binding BrowseCommand}"/>
        </Grid>
        
        <!-- Validation Loading Indicator -->
        <StackPanel Orientation="Horizontal" 
                    Margin="0,15,0,0"
                    Visibility="{Binding IsValidating, Converter={StaticResource BoolToVisibilityConverter}}">
            <ui:ProgressRing IsIndeterminate="True" 
                            Width="20" 
                            Height="20"
                            Margin="0,0,10,0"/>
            <TextBlock Text="{x:Static properties:Resources.StatusValidating}"
                      VerticalAlignment="Center"
                      Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
