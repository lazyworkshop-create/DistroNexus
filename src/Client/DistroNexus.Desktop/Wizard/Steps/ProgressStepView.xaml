<UserControl x:Class="DistroNexus.Desktop.Wizard.Steps.ProgressStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:DistroNexus.Desktop.Wizard.Steps"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance Type=local:ProgressStep}">
    
    <Grid VerticalAlignment="Center">
        <StackPanel HorizontalAlignment="Center" Width="500">
            <!-- Installing Distribution Info -->
            <StackPanel Orientation="Horizontal" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30">
                <Border Width="48" Height="48" 
                        CornerRadius="8"
                        Background="{DynamicResource ControlFillColorDefaultBrush}"
                        Margin="0,0,15,0">
                    <ui:SymbolIcon Symbol="Apps24" 
                                  FontSize="24"
                                  Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Context.SelectedDistribution.Name}"
                              FontSize="18"
                              FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Context.InstanceName}"
                              Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>
            
            <!-- Progress Bar -->
            <ProgressBar Value="{Binding Context.InstallProgress}"
                        Minimum="0"
                        Maximum="100"
                        Height="8"
                        Margin="0,0,0,15"/>
            
            <!-- Progress Percentage -->
            <TextBlock Text="{Binding Context.InstallProgress, StringFormat={}{0:F0}%}"
                      FontSize="24"
                      FontWeight="SemiBold"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,10"/>
            
            <!-- Status Message -->
            <TextBlock Text="{Binding Context.InstallStatusMessage}"
                      FontSize="14"
                      HorizontalAlignment="Center"
                      Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                      TextWrapping="Wrap"
                      TextAlignment="Center"/>
            
            <!-- Cancel Button -->
            <ui:Button Content="Cancel Installation"
                      HorizontalAlignment="Center"
                      Margin="0,30,0,0"
                      Command="{Binding CancelCommand}"
                      Visibility="{Binding CanCancel, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</UserControl>
