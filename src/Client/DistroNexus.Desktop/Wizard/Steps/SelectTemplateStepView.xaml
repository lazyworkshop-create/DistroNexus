<UserControl x:Class="DistroNexus.Desktop.Wizard.Steps.SelectTemplateStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Template List -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <ui:TextBox PlaceholderText="Search templates..." Margin="0,0,10,10" 
                        Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"/>

            <ListView Grid.Row="1" ItemsSource="{Binding Templates}" 
                      SelectedItem="{Binding SelectedTemplate}"
                      SelectionMode="Single"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Description}" TextTrimming="CharacterEllipsis" Opacity="0.7"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
             <!-- Loading Indicator -->
             <ProgressBar Grid.Row="1" IsIndeterminate="True" 
                          VerticalAlignment="Top" 
                          Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>

        <!-- Details -->
        <ScrollViewer Grid.Column="1" Margin="10,0,0,0">
            <Grid>
                 <!-- Fallback text if nothing selected (using data triggers locally to avoid assuming specific converters exist) -->
                 <TextBlock Text="Select a template on the left to view details" 
                            VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.5"/>
                            
                <StackPanel>
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedTemplate}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    
                     <TextBlock Text="{Binding SelectedTemplate.Name}" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
                     <TextBlock Text="{Binding SelectedTemplate.Category}" Foreground="CornflowerBlue" Margin="0,0,0,10"/>
                     
                     <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,10,0,5"/>
                     <TextBlock Text="{Binding SelectedTemplate.Description}" TextWrapping="Wrap"/>
                     
                     <TextBlock Text="Included Software" FontWeight="SemiBold" Margin="0,15,0,5"/>
                     <ItemsControl ItemsSource="{Binding SelectedTemplate.Packages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#11000000" CornerRadius="4" Padding="5,2" Margin="0,2">
                                     <StackPanel Orientation="Horizontal">
                                         <TextBlock Text="{Binding Name}" FontWeight="Medium"/>
                                         <TextBlock Text="{Binding Version, StringFormat=' ({0})'}" Opacity="0.7"/>
                                     </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                     </ItemsControl>

                     <ui:CardAction Icon="Dismiss24" Command="{Binding ClearSelectionCommand}" Content="Do not use a template" Margin="0,20,0,0"
                                    Visibility="Collapsed"/> 
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
