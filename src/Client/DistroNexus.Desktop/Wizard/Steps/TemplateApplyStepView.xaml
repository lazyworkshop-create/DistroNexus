<UserControl x:Class="DistroNexus.Desktop.Wizard.Steps.TemplateApplyStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             lex:ResxLocalizationProvider.DefaultAssembly="DistroNexus.Desktop"
             lex:ResxLocalizationProvider.DefaultDictionary="Properties.Resources"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:DistroNexus.Desktop.Wizard.Steps"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance Type=local:TemplateApplyStep}">

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="10,0,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ProgressBar Grid.Column="0"
                        Value="{Binding Context.InstallProgress}"
                        Minimum="0"
                        Maximum="100"
                        Height="8"
                        Margin="0,0,12,0"/>

            <ui:Button Grid.Column="1"
                      Width="26"
                      Height="26"
                      Padding="0"
                      VerticalAlignment="Center"
                      ToolTip="{lex:Loc CancelInstallation}"
                      Command="{Binding CancelCommand}"
                      Visibility="{Binding CanCancel, Converter={StaticResource BoolToVisibilityConverter}}">
                <ui:SymbolIcon Symbol="Dismiss24"/>
            </ui:Button>
        </Grid>

        <Border Grid.Row="1"
                BorderThickness="1"
                CornerRadius="3"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                VerticalAlignment="Stretch">
            <Grid>
                <ListBox x:Name="TemplateOutputListBox"
                         ItemsSource="{Binding FilteredTemplateOutputLines}"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         HorizontalContentAlignment="Stretch"
                         Padding="4,30,4,4">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}"
                                       TextWrapping="Wrap"
                                       FontFamily="Consolas"
                                       FontSize="11"
                                       LineStackingStrategy="BlockLineHeight"
                                       LineHeight="13"
                                       Margin="2,0,2,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsWarning}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsError}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="8"
                        Padding="4,3"
                        CornerRadius="6"
                        Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
                    <ToggleButton Width="24"
                                  Height="22"
                                  Padding="0"
                                  Background="Transparent"
                                  BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                  Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                  ToolTip="{lex:Loc TooltipOnlyErrors}"
                                  IsChecked="{Binding ShowOnlyErrors, Mode=TwoWay}">
                        <ToggleButton.Style>
                            <Style TargetType="ToggleButton">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ToggleButton.Style>
                        <ui:SymbolIcon Symbol="Warning24"/>
                    </ToggleButton>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
